services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./data/proxy_admin.db
